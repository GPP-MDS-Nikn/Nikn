<% provide(:title, "| Portal") %>

<section id="title" class="emerald">
	<div class="container">
		<div class="row">
			<div class="col-sm-6">
				<h1>Portal de Notícias e Informações</h1>
				<p>Acompanhe as notícias de nosso portal, políticas públicas, legislação, direito e muito mais informações.</p>
			</div>
		</div>
	</div>
</section>

<section id="portfolio" class="container">
    <div class="search-form">
      <%= form_tag(portal_posts_path, method: "get") do %>
        <%= text_field_tag :search, params[:search], placeholder: "Buscar Notícia", class: "form-control search-form-field"%>
        <%= submit_tag "Buscar", class: "search-form-button btn btn-default" %>
      <% end %>
    </div>
    <br>
	<div class="portal-posts">
      <% if @portal_posts.present? %>
		<% @portal_posts.each do |post| %>
			<%= link_to(post) do %>
				<div class="portal-post ">
					<%if post.image_file.present? %>
						<%= image_tag(post.image_file.url(), class: "image center-block") %>
					<%else%>
						<%= image_tag("nikn_default_banner.png", class: "image center-block") %>
					<%end%>
					<p class="title"><%= post.title %></p>
				</div>
			<% end %>
		<% end %>
      <% else %>
        <div class="post-not-found">
          <br />
          <p>Desculpe, nenhuma notícia foi encontrada.</p>
          <br />
        </div>
      <% end %>
	</div>

<% if ong_signed_in? %>
	<a href="/portal_posts/new">
		<button type="button" class="btn btn-default center-block" aria-label="Left Align">
			<p>Nova Postagem</p>
		</button>
	</a>
<% end %>

</section>
