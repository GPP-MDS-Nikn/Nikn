<div class="portal_posts">
	<div class="container">	

		<% @portal_posts.each do |post| %> 
		  <div class="portal_post">
		  	<p class="title"><%= post.title %></p>
		  	<p class="subtitle"><%= post.created_at %>, por <%= post.author %> -</p>
		  	<p class="content"><%= post.content %></p>
		  </div>
		<% end %>
		
		<%= link_to 'New Post', "new" %>
	
	</div>
</div>